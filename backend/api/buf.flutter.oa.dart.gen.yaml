# OA 移動端（Flutter 基座）Dart API 客户端生成模板。
#
# 與 go-wind-cms/backend/api/buf.app.dart.gen.yaml 同構，差異：
#  - inputs 指向 OA 自有的 protos/oa/service/v1（工作流 HTTP 端點）；
#  - out 指向 OA 移動端 frontend/mobile/lib/generated/api。
#
# 这是之前 OA 後端缺失的 Dart 生成链——補齊後移動端可由 buf 直接生成
# 工作流 API 客户端，不再依賴 swagger_parser/openapi-generator 中轉。
#
# 运行：cd backend/api && buf generate --template buf.flutter.oa.dart.gen.yaml

version: v2

clean: true

managed:
  enabled: true

  disable:
    - module: buf.build/googleapis/googleapis
    - module: 'buf.build/envoyproxy/protoc-gen-validate'
    - module: 'buf.build/kratos/apis'
    - module: buf.build/gnostic/gnostic
    - module: buf.build/gogo/protobuf
    - module: buf.build/tx7do/pagination
    - module: buf.build/go-wind/redact

inputs:
  - directory: protos
    paths:
      - protos/oa/service/v1

plugins:
  - local: protoc-gen-dart-http
    out: ../../frontend/mobile/lib/generated/api
